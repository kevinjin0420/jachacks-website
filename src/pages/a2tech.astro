---
import '../styles/site.css';
---

<html lang="en">
  <head>
    <script is:inline>
      try { var t = localStorage.getItem('theme'); if (t === 'light') document.documentElement.setAttribute('data-theme','light'); } catch(e){}
    </script>
    <meta charset="utf-8" />
    <link rel="icon" type="image/png" href="/jaseci-logo.png" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>JacHacks A2Tech 360 · Ann Arbor · Sep 26–27, 2026</title>
    <meta name="description" content="JacHacks A2Tech: Jaseci is the official hackathon partner of a2Tech360 in Ann Arbor, September 26–27, 2026. Registration is now open." />
    <meta name="robots" content="index, follow" />
    <link rel="canonical" href="https://jachacks.org/a2tech" />
    <meta property="og:title" content="JacHacks A2Tech 360 · Ann Arbor" />
    <meta property="og:description" content="Jaseci is the official hackathon partner of a2Tech360. September 26–27, 2026, Ann Arbor." />
    <meta property="og:image" content="https://jachacks.org/jaseci-logo.png" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,700;0,800;0,900;1,700;1,800&family=Rajdhani:wght@400;500;600;700&family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500;600&display=swap" rel="stylesheet" />
  </head>
  <body>

    <div class="orb orb-1"></div>
    <div class="orb orb-2"></div>
    <div class="orb orb-3"></div>
    <div class="orb orb-4"></div>

    <!-- NAV -->
    <nav id="main-nav">
      <div class="nav-inner">
        <a href="/" class="logo">
          <img src="/jaseci-logo.png" alt="JacHacks" class="logo-img" />
          <span class="logo-text">Jac<span class="logo-accent">Hacks</span></span>
        </a>
        <div class="nav-links">
          <a href="/" class="nav-link">// HOME</a>
          <a href="/sf" class="nav-link">// SF</a>
          <a href="/spring" class="nav-link">// SPRING</a>
          <a href="/winter" class="nav-link">// WINTER</a>
          <a href="#past" class="nav-link">// PAST EVENTS</a>
          <a href="/gallery" class="nav-link">// GALLERY</a>
        </div>
        <button class="theme-toggle" id="theme-toggle" aria-label="Toggle theme" title="Toggle light/dark">
          <svg class="tt-sun" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="4"/><path d="M12 2v2M12 20v2M4.93 4.93l1.41 1.41M17.66 17.66l1.41 1.41M2 12h2M20 12h2M4.93 19.07l1.41-1.41M17.66 6.34l1.41-1.41"/></svg>
          <svg class="tt-moon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/></svg>
        </button>
        <a href="https://github.com/jaseci-labs/jaseci" target="_blank" rel="noopener noreferrer" class="nav-cta">STAR ON GITHUB</a>
        <button class="hamburger" id="hamburger" aria-label="Menu">
          <span></span><span></span><span></span>
        </button>
      </div>
    </nav>

    <div class="mobile-menu" id="mobile-menu">
      <a href="/">// HOME</a>
      <a href="/sf">// JACHACKS SF</a>
      <a href="/spring">// SPRING</a>
      <a href="/winter">// WINTER</a>
      <a href="#past">// PAST EVENTS</a>
      <a href="/gallery">// GALLERY</a>
      <a href="https://github.com/jaseci-labs/jaseci" target="_blank" class="mobile-cta">STAR ON GITHUB</a>
    </div>

    <!-- HERO -->
    <section class="hero">
      <div class="hero-grid-bg"></div>
      <canvas id="bg-canvas"></canvas>

      <div class="hud-tl">
        <span class="hud-label">SYS://ANN_ARBOR</span>
      </div>
      <div class="hud-tr">
        <span class="hud-label hud-label-white">EVENT://SEP_26_2026</span>
      </div>
      <div class="hud-bl">
        <span class="hud-label">DURATION: <span class="hud-count">02DAY</span></span>
      </div>
      <div class="hud-br">
        <span class="hud-label hud-label-accent">REGION://MICHIGAN</span>
      </div>

      <div class="float-el fe-hex-1">
        <svg viewBox="0 0 60 69" width="60" height="69" fill="none">
          <polygon points="30,3 57,18 57,51 30,66 3,51 3,18" stroke="rgba(249,115,22,0.3)" stroke-width="1.5"/>
        </svg>
      </div>
      <div class="float-el fe-hex-2">
        <svg viewBox="0 0 40 46" width="40" height="46" fill="none">
          <polygon points="20,2 38,12 38,34 20,44 2,34 2,12" stroke="rgba(0,212,255,0.25)" stroke-width="1.5"/>
        </svg>
      </div>
      <div class="float-el fe-hex-3">
        <svg viewBox="0 0 90 104" width="90" height="104" fill="none">
          <polygon points="45,4 86,27 86,77 45,100 4,77 4,27" stroke="rgba(249,115,22,0.1)" stroke-width="1"/>
        </svg>
      </div>
      <div class="float-el fe-plus-1">+</div>
      <div class="float-el fe-plus-2">+</div>
      <div class="float-el fe-plus-3">×</div>
      <div class="float-el fe-ring-1"></div>
      <div class="float-el fe-ring-2"></div>
      <div class="float-el fe-ring-3"></div>

      <div class="hero-spotlight"></div>

      <!-- Downtown Ann Arbor line-art backdrop -->
      <img class="hero-bridge hero-bridge-a2" src="/a2-lines.png" alt="" aria-hidden="true" style="opacity:.58;" />
      <div class="hero-scrim"></div>

      <div class="hero-content">
        <div class="a2-lockup">
          <span class="a2-lockup-label">OFFICIAL HACKATHON PARTNER OF</span>
          <a href="https://a2tech360.com" target="_blank" rel="noopener noreferrer" class="a2-lockup-link" aria-label="a2Tech360, powered by Ann Arbor SPARK">
            <img src="/a2tech360-logo-white.webp" alt="a2Tech360, powered by Ann Arbor SPARK" class="a2-lockup-logo a2-logo-dark" />
            <img src="/a2tech360-logo-color.webp" alt="" aria-hidden="true" class="a2-lockup-logo a2-logo-light" />
          </a>
        </div>

        <div class="hero-series-badge">
          <span class="series-dot"></span>
          <span>JACHACKS SERIES</span>
          <span class="series-sep">·</span>
          <span class="series-event">ANN ARBOR</span>
        </div>

        <div class="hero-status">
          <span class="hero-status-done">
            <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
            REGISTRATION OPEN
          </span>
          <span class="hero-status-sep"></span>
          <span class="hero-status-next">
            <span class="next-dot"></span>
            SEP 26–27 · ANN ARBOR
          </span>
        </div>

        <div class="hero-title">
          <a href="https://jaclang.org/" target="_blank" rel="noopener noreferrer" class="hero-j-logo-link" aria-label="Jac language">
            <img src="/jaseci-logo.png" alt="" class="hero-j-logo" />
          </a>
          <h1 class="title-main">JacHacks <span class="title-spring">A2Tech</span></h1>
        </div>

        <p class="hero-sub">The series lands in Ann Arbor for <a href="https://a2tech360.com" target="_blank" rel="noopener noreferrer" class="text-link">a2Tech360</a>, the region's biggest tech week. Two days to build the future with Jac.</p>

        <div class="hero-meta-line">
          <span>SEP 26–27, 2026</span>
          <span class="meta-dot">·</span>
          <span>ANN ARBOR, MI</span>
        </div>

        <div class="countdown" id="countdown">
          <div class="cd-block">
            <span class="cd-val" id="cd-days">00</span>
            <span class="cd-label">DAYS</span>
          </div>
          <span class="cd-sep">:</span>
          <div class="cd-block">
            <span class="cd-val" id="cd-hours">00</span>
            <span class="cd-label">HRS</span>
          </div>
          <span class="cd-sep">:</span>
          <div class="cd-block">
            <span class="cd-val" id="cd-mins">00</span>
            <span class="cd-label">MIN</span>
          </div>
          <span class="cd-sep">:</span>
          <div class="cd-block">
            <span class="cd-val" id="cd-secs">00</span>
            <span class="cd-label">SEC</span>
          </div>
        </div>

        <div class="cta-group">
          <a href="https://luma.com/jlpxupsw" target="_blank" rel="noopener noreferrer" class="btn-primary btn-register">
            <span class="cs-dot"></span>
            REGISTER NOW
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
          </a>
          <a href="#past" class="btn-ghost">
            EXPLORE PAST JACHACKS
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M12 5v14M5 12l7 7 7-7"/></svg>
          </a>
        </div>
      </div>

      <div class="scroll-down">
        <span>SCROLL</span>
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 5v14M5 12l7 7 7-7"/></svg>
      </div>
    </section>

    <!-- ABOUT a2Tech360 -->
    <section class="section" id="about">
      <div class="container">
        <div class="section-header reveal" style="text-align:center; display:flex; flex-direction:column; align-items:center;">
          <div class="section-eyebrow">// OFFICIAL HACKATHON PARTNER</div>
          <h2 class="section-title">Ann Arbor's tech week, <span class="text-accent">hacked</span></h2>
          <p class="section-sub" style="margin:0 auto;">Jaseci is the official hackathon partner of a2Tech360, Ann Arbor's premier technology festival, a week of experiences, conversations, and connections across the region's innovation ecosystem. JacHacks A2Tech is that hackathon: two days of building with Jac and agentic AI, September 26–27.</p>
        </div>
      </div>
    </section>

    <!-- PAST JACHACKS -->
    <section class="section section-dark" id="past">
      <div class="container">
        <div class="section-header reveal">
          <div class="section-eyebrow">// THE SERIES SO FAR</div>
          <h2 class="section-title">Past <span class="text-accent">JacHacks</span></h2>
          <p class="section-sub">Step into the projects, winners, and moments from every JacHacks so far.</p>
        </div>

        <div class="editions-grid reveal">
          <a href="/spring" class="edition-card edition-card-spring">
            <div class="edition-card-bg"></div>
            <div class="edition-card-inner">
              <div class="edition-card-tag">GLOBAL · ONLINE</div>
              <h3 class="edition-card-title">JacHacks <span>Spring</span></h3>
              <div class="edition-card-meta">MAY 2026</div>
              <div class="edition-card-stats">
                <span><b>300+</b> builders</span>
                <span><b>81</b> projects</span>
                <span><b>13</b> winners</span>
              </div>
              <span class="edition-card-cta">EXPLORE SPRING
                <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M13 5l7 7-7 7"/></svg>
              </span>
            </div>
          </a>

          <a href="/winter" class="edition-card edition-card-winter">
            <div class="edition-card-bg" style="background-image:url(/photos/DSC06792.webp)"></div>
            <div class="edition-card-inner">
              <div class="edition-card-tag">ANN ARBOR, MI</div>
              <h3 class="edition-card-title">JacHacks <span>Winter</span></h3>
              <div class="edition-card-meta">APRIL 2026</div>
              <div class="edition-card-stats">
                <span><b>170+</b> hackers</span>
                <span><b>70</b> projects</span>
                <span><b>$5K</b> prizes</span>
              </div>
              <span class="edition-card-cta">EXPLORE WINTER
                <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M13 5l7 7-7 7"/></svg>
              </span>
            </div>
          </a>
        </div>

        <div class="editions-grid reveal" style="margin-top:1.5rem;">
          <a href="/paradise" class="edition-card edition-card-paradise">
            <div class="edition-card-bg"></div>
            <div class="edition-card-inner">
              <div class="edition-card-tag">MIAMI BEACH, FL</div>
              <h3 class="edition-card-title">Velric × <span>The Foundry</span></h3>
              <div class="edition-card-meta">MIAMI HACKATHON · VENTURE WEEK 2026</div>
              <div class="edition-card-stats">
                <span><b>Host</b> AI track</span>
                <span><b>Keynote</b> Jason Mars</span>
                <span><b>Judge</b> demos</span>
              </div>
              <span class="edition-card-cta">EXPLORE EVENT
                <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M13 5l7 7-7 7"/></svg>
              </span>
            </div>
          </a>

          <a href="/ohio" class="edition-card edition-card-ohio">
            <div class="edition-card-bg"></div>
            <div class="edition-card-inner">
              <div class="edition-card-tag">TOLEDO, OH</div>
              <h3 class="edition-card-title">Rocket<span>Hacks</span></h3>
              <div class="edition-card-meta">MAR 14–15, 2026 · UTOLEDO</div>
              <div class="edition-card-stats">
                <span><b>Keynote</b> Jason Mars</span>
                <span><b>Prize</b> agentic AI</span>
                <span><b>Sponsor</b> Jaseci</span>
              </div>
              <span class="edition-card-cta">EXPLORE EVENT
                <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M13 5l7 7-7 7"/></svg>
              </span>
            </div>
          </a>
        </div>

        <a href="/gallery" class="editions-gallery reveal" style="margin-top:1.5rem;">
          <div class="editions-gallery-photos">
            <img src="/photos/DSC06792.webp" alt="" loading="lazy" />
            <img src="/photos/DSC06776.webp" alt="" loading="lazy" />
            <img src="/photos/DSC06646.webp" alt="" loading="lazy" />
            <img src="/photos/DSC06563.webp" alt="" loading="lazy" />
          </div>
          <div class="editions-gallery-content">
            <div class="edition-card-tag">PHOTOS</div>
            <h3 class="editions-gallery-title">Explore the <span class="text-accent">Gallery</span></h3>
            <p class="editions-gallery-sub">Albums from Ann Arbor, Miami, and Toledo.</p>
            <span class="edition-card-cta">OPEN GALLERY
              <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M13 5l7 7-7 7"/></svg>
            </span>
          </div>
        </a>
      </div>
    </section>

    <!-- FOOTER -->
    <footer>
      <div class="footer-top">
        <div class="footer-logo">
          <img src="/jaseci-logo.png" alt="Jaseci" class="logo-img" />
          <span>Jac<span class="text-accent">Hacks</span></span>
        </div>
        <div class="footer-nav">
          <a href="/">JacHacks SF</a>
          <a href="/sf">JacHacks SF</a>
          <a href="/spring">JacHacks Spring</a>
          <a href="/winter">JacHacks Winter</a>
          <a href="/gallery">Gallery</a>
        </div>
        <div class="footer-socials">
          <a href="https://x.com/Jaseci_Org" class="social-btn" aria-label="Twitter">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor"><path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/></svg>
          </a>
          <a href="https://www.instagram.com/jaseci.ai/" target="_blank" class="social-btn" aria-label="Instagram">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="2" y="2" width="20" height="20" rx="5"/><circle cx="12" cy="12" r="4"/><circle cx="17.5" cy="6.5" r="1" fill="currentColor"/></svg>
          </a>
          <a href="https://www.linkedin.com/company/jaseci-labs/posts/?feedView=all" target="_blank" rel="noopener noreferrer" class="social-btn" aria-label="LinkedIn">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor"><path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-4 0v7H10v-7a6 6 0 0 1 6-6zM2 9h4v12H2z"/><circle cx="4" cy="4" r="2"/></svg>
          </a>
        </div>
      </div>
      <div class="footer-bottom">
        <p>© 2026 JacHacks · Powered by <a href="https://jaclang.org/" target="_blank" rel="noopener noreferrer">Jaseci</a> · A global hackathon series</p>
      </div>
    </footer>

  <style is:global>
    /* the note between the countdown and the buttons is gone, so restore that breathing room */
    .hero-content .cta-group { margin-top: 2rem; }

    /* the hero Jac mark links out to jaclang.org */
    .hero-j-logo-link { display: block; line-height: 0; transition: transform .3s ease, filter .3s ease; }
    .hero-j-logo-link:hover { transform: translateY(-3px); }
    .hero-j-logo-link:hover .hero-j-logo { filter: drop-shadow(0 0 28px rgba(249,115,22,.8)) drop-shadow(0 0 70px rgba(249,115,22,.3)); }
    .hero-j-logo { transition: filter .3s ease; }

    /* a2Tech360 partner lockup, sits at the very top of the hero */
    .a2-lockup {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: .7rem;
      margin-bottom: 1.6rem;
    }
    .a2-lockup-label {
      font-family: var(--mono);
      font-size: .58rem;
      letter-spacing: .28em;
      color: rgba(242,234,214,.4);
    }
    .a2-lockup-link { display: block; line-height: 0; transition: transform .3s ease, filter .3s ease; }
    .a2-lockup-link:hover { transform: translateY(-2px); }
    .a2-lockup-link:hover .a2-lockup-logo { opacity: 1; }
    .a2-lockup-logo {
      height: 2.7rem;
      width: auto;
      object-fit: contain;
      opacity: .88;
      transition: opacity .3s ease;
    }

    /* full-colour mark on the light theme, white knockout on the dark theme */
    .a2-logo-light { display: none; }
    :root[data-theme="light"] .a2-logo-dark { display: none; }
    :root[data-theme="light"] .a2-logo-light { display: block; }
    :root[data-theme="light"] .a2-lockup-label { color: rgba(26,24,40,.5); }
    :root[data-theme="light"] .a2-lockup-logo { opacity: .95; }

    @media (max-width: 560px) {
      .a2-lockup { gap: .5rem; margin-bottom: 1.1rem; }
      .a2-lockup-label { font-size: .52rem; letter-spacing: .22em; }
      .a2-lockup-logo { height: 2.05rem; }
    }
  </style>

  <script>
    // ===== CANVAS =====
    const canvas = document.getElementById('bg-canvas') as HTMLCanvasElement;
    const ctx = canvas.getContext('2d')!;
    const OR = 'rgba(249,115,22,';
    const CY = 'rgba(0,212,255,';

    function resize() { canvas.width = canvas.offsetWidth; canvas.height = canvas.offsetHeight; }
    resize();
    window.addEventListener('resize', resize);

    const N = 35;
    interface GNode { x:number;y:number;vx:number;vy:number;r:number;cyan:boolean; }
    interface Walker { fi:number;ti:number;t:number;sp:number; }
    interface Particle { x:number;y:number;vx:number;vy:number;a:number;sz:number;life:number;ml:number; }

    const nodes: GNode[] = Array.from({length:N}, () => ({
      x: Math.random()*canvas.width, y: Math.random()*canvas.height,
      vx: (Math.random()-.5)*.3, vy: (Math.random()-.5)*.3,
      r: Math.random()*2+1.5, cyan: Math.random()<.22
    }));
    const walkers: Walker[] = Array.from({length:6}, () => ({
      fi: Math.floor(Math.random()*N), ti: Math.floor(Math.random()*N), t:0, sp:.005+Math.random()*.004
    }));
    const parts: Particle[] = [];
    function sp(): Particle {
      return {x:Math.random()*canvas.width,y:canvas.height+10,vx:(Math.random()-.5)*.4,vy:-(0.3+Math.random()*.5),a:.4+Math.random()*.4,sz:Math.random()*1.5+.5,life:0,ml:200+Math.random()*200};
    }
    for(let i=0;i<60;i++){const p=sp();p.y=Math.random()*canvas.height;p.life=Math.random()*p.ml;parts.push(p);}

    const ED=220;
    function draw(){
      const W=canvas.width,H=canvas.height;
      ctx.clearRect(0,0,W,H);
      for(const n of nodes){n.x+=n.vx;n.y+=n.vy;if(n.x<0||n.x>W)n.vx*=-1;if(n.y<0||n.y>H)n.vy*=-1;}
      for(let i=0;i<nodes.length;i++){
        for(let j=i+1;j<nodes.length;j++){
          const dx=nodes[i].x-nodes[j].x,dy=nodes[i].y-nodes[j].y,d=Math.sqrt(dx*dx+dy*dy);
          if(d<ED){
            const a=(1-d/ED)*.15;
            const c=nodes[i].cyan&&nodes[j].cyan?CY:OR;
            ctx.beginPath();ctx.moveTo(nodes[i].x,nodes[i].y);ctx.lineTo(nodes[j].x,nodes[j].y);
            ctx.strokeStyle=`${c}${a})`;ctx.lineWidth=.8;ctx.stroke();
          }
        }
      }
      for(const n of nodes){
        ctx.beginPath();ctx.arc(n.x,n.y,n.r,0,Math.PI*2);
        ctx.fillStyle=n.cyan?`${CY}.45)`:`${OR}.4)`;ctx.fill();
      }
      for(const w of walkers){
        w.t+=w.sp;
        if(w.t>=1){
          w.fi=w.ti;
          const from=nodes[w.fi];let best=-1,bd=Infinity;
          for(let i=0;i<N;i++){if(i===w.fi)continue;const dx=from.x-nodes[i].x,dy=from.y-nodes[i].y,d=Math.sqrt(dx*dx+dy*dy);if(d<ED&&d<bd){bd=d;best=i;}}
          w.ti=best===-1?Math.floor(Math.random()*N):best;w.t=0;
        }
        const f=nodes[w.fi],t=nodes[w.ti];
        const wx=f.x+(t.x-f.x)*w.t,wy=f.y+(t.y-f.y)*w.t;
        const c=nodes[w.fi].cyan?CY:OR;
        const g=ctx.createRadialGradient(wx,wy,0,wx,wy,12);
        g.addColorStop(0,`${c}.85)`);g.addColorStop(1,`${c}0)`);
        ctx.beginPath();ctx.arc(wx,wy,12,0,Math.PI*2);ctx.fillStyle=g;ctx.fill();
        ctx.beginPath();ctx.arc(wx,wy,2.5,0,Math.PI*2);ctx.fillStyle=`${c}1)`;ctx.fill();
      }
      if(Math.random()<.3)parts.push(sp());
      for(let i=parts.length-1;i>=0;i--){
        const p=parts[i];p.x+=p.vx;p.y+=p.vy;p.life++;
        const fade=Math.min(p.life/30,1)*Math.max(1-p.life/p.ml,0);
        ctx.beginPath();ctx.arc(p.x,p.y,p.sz,0,Math.PI*2);
        ctx.fillStyle=`${OR}${p.a*fade})`;ctx.fill();
        if(p.life>=p.ml)parts.splice(i,1);
      }
      requestAnimationFrame(draw);
    }
    draw();

    // ===== COUNTDOWN (JacHacks A2Tech) =====
    (function() {
      // 9:00 AM ET (EDT, UTC-4) on Sat, Sep 26, 2026
      const target = new Date('2026-09-26T13:00:00Z').getTime();
      const days = document.getElementById('cd-days');
      const hours = document.getElementById('cd-hours');
      const mins = document.getElementById('cd-mins');
      const secs = document.getElementById('cd-secs');
      if (!days || !hours || !mins || !secs) return;
      const pad = (n: number) => String(n).padStart(2, '0');
      function tick() {
        const diff = target - Date.now();
        if (diff <= 0) { days!.textContent = '0'; hours!.textContent = mins!.textContent = secs!.textContent = '00'; return; }
        days!.textContent  = String(Math.floor(diff / 86400000));
        hours!.textContent = pad(Math.floor((diff % 86400000) / 3600000));
        mins!.textContent  = pad(Math.floor((diff % 3600000) / 60000));
        secs!.textContent  = pad(Math.floor((diff % 60000) / 1000));
      }
      tick();
      setInterval(tick, 1000);
    })();

    // ===== REVEAL =====
    const obs=new IntersectionObserver(es=>{es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('revealed');obs.unobserve(e.target);}});},{threshold:.08,rootMargin:'0px 0px -40px 0px'});
    document.querySelectorAll('.reveal').forEach(el=>obs.observe(el));

    // ===== NAV =====
    const nav=document.getElementById('main-nav')!;
    window.addEventListener('scroll',()=>nav.classList.toggle('scrolled',window.scrollY>60));

    // ===== HAMBURGER =====
    const hbg=document.getElementById('hamburger')!,mm=document.getElementById('mobile-menu')!;
    hbg.addEventListener('click',()=>{hbg.classList.toggle('open');mm.classList.toggle('open');});
    mm.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{hbg.classList.remove('open');mm.classList.remove('open');}));

    // ===== THEME TOGGLE =====
    const themeBtn = document.getElementById('theme-toggle')!;
    themeBtn.addEventListener('click', () => {
      const isLight = document.documentElement.getAttribute('data-theme') === 'light';
      if (isLight) {
        document.documentElement.removeAttribute('data-theme');
        try { localStorage.setItem('theme','dark'); } catch(e){}
      } else {
        document.documentElement.setAttribute('data-theme','light');
        try { localStorage.setItem('theme','light'); } catch(e){}
      }
    });
  </script>
  </body>
</html>
